# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
env/
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
share/python-wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# Node
node_modules/
npm-debug.log
yarn-error.log
.next/
out/
build/

# OS
.DS_Store
Thumbs.db

# Local secrets — never commit
.env
.env.*

infra/nginx/certs/*.pem
infra/nginx/certs/*.crt

# graft's local graph cache - regenerable, not committed (run `graft build`).
/graft/
# ORION corpus build artifacts (large, regenerable)
data/training/corpus/train-*.jsonl
data/training/corpus/val-*.jsonl
data/training/corpus/test-*.jsonl
data/training/corpus/.cache-build/

# SQLite development databases
*.db

# Terraform state — contains secrets, NEVER commit
*.tfstate
*.tfstate.*

# Model weights — too large for git
*.gguf
*.safetensors
*.bin
models/checkpoints/
models/comp001/

# Large binary docs — host externally
*.pdf

# Local caches (regenerable)
.cache/

# Model weights (any torch checkpoint)
*.pt
models/hive/
